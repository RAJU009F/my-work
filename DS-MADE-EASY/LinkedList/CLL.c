#include<stdio.h>
#include<stdlib.h>
#include<stdbool.h>
#include<limits.h>
/*

# Author			: @RAJ009F
# Topic or Type 	: LinkedLists
# Problem Statement	: 
# Description		:
# Complexity		:
=======================
#sample output
----------------------

=======================
*/

struct CLL
{
	int data;
	struct CLL *next;
};

void push(struct CLL **h, int data)
{
	struct CLL *node = (struct CLL *)malloc(struct sizeof(struct CLL));
	
	
	
	
	
}

int main()
	{
		
		struct CLL *head = NULL;
		push(&head, 12);
	
	
	
	return 0;
	}